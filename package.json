{
  "name": "claude-max-api-proxy",
  "version": "1.0.0",
  "description": "OpenAI-compatible API proxy for Claude Max subscription via Claude Code CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "claude-max-proxy": "dist/server/standalone.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server/standalone.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["claude", "openai", "api", "proxy", "clawdbot"],
  "author": "Ghost Works",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "peerDependencies": {
    "expect": "*"
  },
  "peerDependenciesMeta": {
    "expect": {
      "optional": false
    }
  }
}
